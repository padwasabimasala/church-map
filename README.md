# churches.geojson

Run `python3 generate_geojson.py` in this directory to generate `churches.geojson` from `output.json`.

The resulting file is a GeoJSON FeatureCollection of Points suitable for loading with Leaflet's `L.geoJSON()`.
